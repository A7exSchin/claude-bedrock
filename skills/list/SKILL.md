---
description: List every configured bedrock tier (core, project, modes, session notes) with state indicators for what's active, inactive, or missing.
disable-model-invocation: true
---

!`node "${CLAUDE_PLUGIN_ROOT}/scripts/bedrock.js" list "${CLAUDE_PROJECT_DIR}" "${CLAUDE_SESSION_ID}"`

Show the listing above to the user as-is.
