---
description: List every configured bedrock tier (core, project, modes, session notes) with state indicators for what's active, inactive, or missing.
disable-model-invocation: true
---

!`node "${CLAUDE_PLUGIN_ROOT}/scripts/bedrock.js" list "${CLAUDE_PROJECT_DIR}" "${CLAUDE_SESSION_ID}"`

Reproduce the text above verbatim as your entire response — every line, unmodified,
including every bullet, state indicator (●/○/?), and file path, in a fenced code block.
Do not summarize, paraphrase, condense, or add commentary before or after it.
